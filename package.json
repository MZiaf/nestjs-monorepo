{
  "name": "nestjs-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "client"
  ],
  "scripts": {
    "clean": "lerna clean",
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build"
  },
  "devDependencies": {
    "lerna": "^6.0.1"
  }
}
